# KOZO v1.0.0-rc.1 Evidence

Status: technical hardening and final promotion readiness complete; tag and
prerelease publication require separate authorization.

## Candidate Identity

* Version authority: `release/version.txt`
* Display version: `v1.0.0-rc.1`
* Archive name: `kozo-v1.0.0-rc.1.tar.xz`
* Commit: recorded from `git rev-parse HEAD` in generated release metadata
* Published: `false`

## Verification Baseline

The accepted v0.8.9 baseline is commit
`cd0ef004690b5a509507406ac8d1a465ebac0104`.

Hosted runs:

* CI: `30398906417`, success
* Lint: `30398906359`, success
* Verification: 67 checks, 0 failures
* QEMU: pass
* Blocker: none
* Runtime markers: 41

The release-candidate source commit must reproduce these results before
promotion.

## Licenses

* Repository license set: MIT OR Apache-2.0
* Core service package selection: MIT
* Included files: `LICENSE`, `LICENSE-MIT`, `LICENSE-APACHE`
* NOTICE: not present and not required by current repository policy

The packager compares the bundled legal files byte-for-byte with the checked
out commit.

## Branch Protection Review

Reviewed and updated through the GitHub API on 2026-07-28:

* initial `main` protection: absent;
* initial repository rulesets: none;
* final force-push setting: blocked;
* required status checks: disabled;
* pull-request reviews: disabled;
* administrator enforcement: disabled;
* conversation resolution, linear history, signatures, and locking: disabled;
* branch deletion: allowed;
* final repository rulesets: none.

REST and GraphQL live readback agree on the final state. This phase changed no
other repository policy.

## CI Warning Review

| Warning | Source | Action | Status |
| --- | --- | --- | --- |
| Node.js 20 deprecation | `actions/checkout@v4` | Move to official `actions/checkout@v7` | source correction pending hosted proof |
| Node.js 20 and Node API deprecations | `actions/upload-artifact@v4` | Move to official `actions/upload-artifact@v7` | source correction pending hosted proof |
| Invalid access token, Git fallback | `laytan/setup-odin@v2` | Pass the existing workflow `GITHUB_TOKEN` | source correction pending hosted proof |

Workflow names, job names, permissions, and artifact names remain stable.

## Dry-Run Evidence

The source-controlled manifest is `release/release_files.v1.json`. The
generated `release_metadata.json` records the candidate commit, branch,
verification result, QEMU result, marker count, legal set, artifact names,
checksum algorithm, and `published: false`.

Two packaging runs from one commit must have the same path list, required
artifact names, legal bytes, and release-note bytes. Build timestamps and
generated verification timestamps may differ, so this phase does not claim
bit-for-bit reproducible archives.

## Publication Status

No tag, release, package publication, upload to a public registry, signature,
or final v1.0.0 approval is authorized by this phase.
