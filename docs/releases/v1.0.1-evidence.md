# KOZO v1.0.1 Release Evidence

Status: Patch release prepared; publication pending

This record separates pre-publication bundle evidence from the later immutable
GitHub release record. Hosted fields are completed only after the accepted
commit is tagged and the downloaded assets are independently verified.

## Release Identity

| Field | Value |
| --- | --- |
| Version | `1.0.1` |
| Display version | `v1.0.1` |
| Tag | `v1.0.1` |
| Release title | `KOZO v1.0.1` |
| Draft | `false` |
| Prerelease | `false` |

## Defect Scope

`KOZO-TRIAGE-001` is the only authorized correction. It is classified as
`BUILD_TOOLING`, user impact `U2`, and release severity `R3`.

The fixed boundary accepts only the canonical requested path and its exact
`.o` and legacy `.obj` alternatives. It rejects stale, missing, ambiguous,
symlink, and non-regular output without adding host or Odin-version branches.

## Commit

The exact accepted commit and annotated tag object are recorded after hosted
CI acceptance and publication. The tag must target a new hosted-accepted
descendant of patch-preparation baseline
`b2bc296dbfd7fdeda5c6ab48c861ff569850f350`.

## Version

`release/version.txt` is the authoritative version source. The packager,
archive name, release metadata, release notes, and CI bundle gate consume
`1.0.1`.

## Verification Requirements

The release commit must pass:

```text
Python tests: pass
Odin: pass
Cargo: pass
cargo-deny: pass
cargo-audit: pass
verification: 67 checks, 0 failures
QEMU: pass
blocker: none
markers: 41
final marker: KOZO_RUNTIME_RETURN_OK
```

Focused object-output tests must cover exact, `.o`, and `.obj` success;
compiler failure; missing and ambiguous output; stale output; invalid output
type; and paths containing spaces.

Local preparation passed 27 focused tests and 1,058 full Python tests. Governed
verification run `verify-20260807T213826Z` passed 67 checks with zero failures;
QEMU reported pass, blocker none, and 41 markers ending at
`KOZO_RUNTIME_RETURN_OK`.

## Runtime Preservation

The patch changes no kernel runtime, userspace runtime, ABI, contracts,
schemas, marker taxonomy, page mappings, capabilities, or halt behavior.

## Artifact List

The approved hosted asset set is:

* `kozo-v1.0.1.tar.xz`
* `kozo.iso`
* `kozo-kernel.elf`
* `latest_verify.json`
* `release_metadata.json`
* `SHA256SUMS`

Sizes and SHA-256 values are recorded only after the final bundle and hosted
downloads are independently inspected.

## Checksums

The pre-publication bundle and downloaded hosted assets must both pass
`SHA256SUMS`. The hosted archive must match the approved local archive.

## Hosted Gate

The exact release commit must pass both CI and lint before the annotated tag
is created. The decisive `Build and validate release bundle` step must pass
with the current Odin toolchain. Run identifiers are recorded after
acceptance.

## Tag And Release Rules

The final tag must be annotated, unsigned, and target the exact hosted-approved
release commit. Local Git, remote Git, and GitHub API readback must agree.

The GitHub release must be final, non-draft, and non-prerelease. It must use
these release notes and upload exactly the six approved assets without
replacement or `--clobber`.

## Prior Release Immutability

The existing release records must remain unchanged:

* `v1.0.0` annotated tag object
  `059fe90572db46185c219e8b38bbd190faa40e60`, targeting
  `1586089415a98a11d2024d606ce6301f568b7d6e`;
* `v1.0.0-rc.1` annotated tag object
  `f4c500b9215869a62dc5dce2b2f655d6a2277be4`, targeting
  `2debd995634d977458de0c1697f21dc0016b4bf9`;
* both hosted release classifications, notes, and six-asset sets.

## Publication Status

Current published release: `v1.0.0`.

`v1.0.1` is prepared for local and hosted validation. Publication remains
pending until the authorized annotated tag, final GitHub release, and hosted
asset verification complete.

Generated release metadata retains `published: false` because the bundle is
built before publication. The later immutable GitHub release record documents
actual publication without rewriting the bundle.
