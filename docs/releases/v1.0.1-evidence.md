# KOZO v1.0.1 Release Evidence

Status: Published final patch release; hosted assets independently verified

This record connects the pre-publication bundle evidence to the immutable
GitHub release. The release was created only after the exact tag target passed
local and hosted gates.

## Release Identity

| Field | Value |
| --- | --- |
| Version | `1.0.1` |
| Display version | `v1.0.1` |
| Tag | `v1.0.1` |
| Release title | `KOZO v1.0.1` |
| Draft | `false` |
| Prerelease | `false` |
| Release URL | <https://github.com/irgordon/kozo/releases/tag/v1.0.1> |
| Published | `2026-08-07T22:45:42Z` |

## Defect Scope

`KOZO-TRIAGE-001` is the only authorized correction. It is classified as
`BUILD_TOOLING`, user impact `U2`, and release severity `R3`.

The fixed boundary accepts only the canonical requested path and its exact
`.o` and legacy `.obj` alternatives. It rejects stale, missing, ambiguous,
symlink, and non-regular output without adding host or Odin-version branches.

## Commit

The exact accepted commit is
`02f1b0113458b988562b7e03362ec9ae716cebd0`. It is a new descendant of
patch-preparation baseline
`b2bc296dbfd7fdeda5c6ab48c861ff569850f350`.

Unsigned annotated tag object
`7896c767376a666ad7f7d8f294beadb473b0290c` targets that commit. Local Git,
remote Git, and GitHub API readback agree.

## Version

`release/version.txt` is the authoritative version source. The packager,
archive name, release metadata, release notes, and CI bundle gate consume
`1.0.1`.

## Verification Requirements

The release commit must pass:

```text
Python tests: pass
Odin: pass
Cargo: pass
cargo-deny: pass
cargo-audit: pass
verification: 67 checks, 0 failures
QEMU: pass
blocker: none
markers: 41
final marker: KOZO_RUNTIME_RETURN_OK
```

Focused object-output tests must cover exact, `.o`, and `.obj` success;
compiler failure; missing and ambiguous output; stale output; invalid output
type; and paths containing spaces.

Local preparation passed 27 focused tests and 1,058 full Python tests. Governed
verification run `verify-20260807T214144Z` passed 67 checks with zero failures;
QEMU reported pass, blocker none, and 41 markers ending at
`KOZO_RUNTIME_RETURN_OK`.

## Runtime Preservation

The patch changes no kernel runtime, userspace runtime, ABI, contracts,
schemas, marker taxonomy, page mappings, capabilities, or halt behavior.

## Artifact List

The approved and published asset set is:

| Asset | Size | SHA-256 |
| --- | ---: | --- |
| `kozo-v1.0.1.tar.xz` | 423952 | `a516e7d020f21ec31950cf607dbdf2155900d04d73c78e670cd737624f62514b` |
| `kozo.iso` | 2904064 | `c50c758f49731dded1175e9b14e42bccfc5b62552bcc1c6b5708803ef40ff03e` |
| `kozo-kernel.elf` | 74368 | `c18f28d4e585a29f642fc20db0d6df2b8ae2041c4b108bc76cccda43ad2a7dcc` |
| `latest_verify.json` | 18061 | `a57821f6d53b71434cdec309affbdf45397854130c655a53cbb9bd5a08eeeb77` |
| `release_metadata.json` | 604 | `f7fbacba82ea9e22f5b4d303f624d1eb807c855f33447119674509b293aa7ec7` |
| `SHA256SUMS` | 415 | `0dcacfe4d13d7ccd6e930e82bb7e3ba5f6d1b4310c3b381660b2a60cf42e001d` |

## Checksums

The local macOS bundle and hosted Linux bundle both passed their own
`SHA256SUMS`, metadata, license, and prohibited-path checks. Their bytes differ
because the build products and timestamps are host-produced; no reproducible
binary claim is made.

The independently downloaded GitHub release matches the selected hosted-CI
publication bundle byte for byte. Its `SHA256SUMS` validates all five covered
assets.

## Hosted Gate

CI run `31223062540` and lint run `31223061928` passed at the exact release
commit before tag creation. CI completed the decisive
`Build and validate release bundle` step with the current Odin toolchain and
uploaded the selected dry-run bundle.

## Tag And Release Rules

The final tag is annotated, unsigned, and targets the exact hosted-approved
release commit. Local Git, remote Git, and GitHub API readback agree.

The GitHub release is final, non-draft, and non-prerelease. Hosted notes match
`docs/releases/v1.0.1.md`, and exactly the six approved assets were uploaded
without replacement or `--clobber`.

## Prior Release Immutability

The existing release records must remain unchanged:

* `v1.0.0` annotated tag object
  `059fe90572db46185c219e8b38bbd190faa40e60`, targeting
  `1586089415a98a11d2024d606ce6301f568b7d6e`;
* `v1.0.0-rc.1` annotated tag object
  `f4c500b9215869a62dc5dce2b2f655d6a2277be4`, targeting
  `2debd995634d977458de0c1697f21dc0016b4bf9`;
* both hosted release classifications, notes, and six-asset sets.

## Publication Status

Current published release: `v1.0.1`.

Previous final release: `v1.0.0`. Previous prerelease: `v1.0.0-rc.1`.

Generated release metadata retains `published: false` because the bundle is
built before publication. The later immutable GitHub release record documents
actual publication without rewriting the bundle.

The downloaded hosted ISO passed the governed QEMU smoke path with blocker
none, 41 ordered markers, and final marker `KOZO_RUNTIME_RETURN_OK`.
`KOZO-TRIAGE-001` is resolved in v1.0.1. `core_service` was not published.
