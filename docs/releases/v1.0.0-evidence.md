# KOZO v1.0.0 Release Evidence

Status: Final release preparation in progress

This record separates pre-publication bundle evidence from the later immutable
GitHub release record. Hosted fields are completed only after the accepted
commit is tagged and the downloaded assets are verified.

## Release Identity

| Field | Value |
| --- | --- |
| Version | `1.0.0` |
| Display version | `v1.0.0` |
| Tag | `v1.0.0` |
| Release title | `KOZO v1.0.0` |
| Draft | `false` |
| Prerelease | `false` |

## Commit

The exact accepted commit and annotated tag object are recorded after hosted
CI acceptance and publication. The release must target the same commit used to
build the approved final bundle.

## Version

`release/version.txt` is the authoritative version source. The packager,
archive name, release metadata, notes, and CI bundle gate consume `1.0.0`.

## Verification Result

Local governed verification passed:

```text
67 checks
0 failures
```

Local run ID: `verify-20260729T201733Z`.

The hosted run identifier is recorded after acceptance.

## QEMU Result

Local QEMU evidence passed:

```text
outcome: pass
blocker: none
final marker: KOZO_RUNTIME_RETURN_OK
```

## Marker Count

The governed runtime sequence contains 41 ordered markers. Runtime marker
ownership and order remain unchanged from `v1.0.0-rc.1`.

## Cargo Policy

The release gate requires:

```text
cargo check: pass
cargo-deny: pass
cargo-audit: pass
```

The `core_service` package is not published.

## Licenses

Repository license set: MIT OR Apache-2.0.

The bundle must contain byte-identical copies of:

* `LICENSE`
* `LICENSE-MIT`
* `LICENSE-APACHE`

The `core_service` package selects MIT.

## Artifact List

The approved hosted asset set is:

* `kozo-v1.0.0.tar.xz`
* `kozo.iso`
* `kozo-kernel.elf`
* `latest_verify.json`
* `release_metadata.json`
* `SHA256SUMS`

Sizes and SHA-256 values are recorded after the final bundle and hosted
downloads are independently inspected.

## Checksums

The pre-publication bundle and the downloaded hosted assets must both pass
`SHA256SUMS`. The hosted archive hash must match the approved local archive.

## Hosted CI

The final commit must pass both the CI and lint workflows before `v1.0.0` is
tagged. Run URLs and results are recorded after acceptance.

## Tag Verification

The final tag must be annotated, unsigned, and target the accepted final
commit. Local Git, remote Git, and GitHub API readback must agree.

## Hosted Asset Verification

The release is created only after the remote tag is verified. All six hosted
assets are downloaded into a new directory and checked independently.

## RC Immutability Check

The existing `v1.0.0-rc.1` record must remain unchanged:

* tag object `f4c500b9215869a62dc5dce2b2f655d6a2277be4`;
* target commit `2debd995634d977458de0c1697f21dc0016b4bf9`;
* prerelease true and draft false;
* six hosted assets;
* archive SHA-256
  `dc3e4dc98d97c15c4b1c7d47456dbc505456e0ddecc88fd20031f9350bb2b9fe`.

## Publication Status

Publication is pending final bundle inspection and the hosted release gate.

Generated `release_metadata.json` retains `published: false` because the
bundle is built before publication. The later GitHub release record documents
actual publication and does not rewrite the accepted bundle.
