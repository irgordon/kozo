# KOZO v1.0.1 Post-Release Observation

## Purpose

This report records the bounded post-release observation of KOZO `v1.0.1`.
It checks the immutable release, project-visible reports, hosted workflows,
published artifacts, the downloaded ISO, and the cross-host Odin object
boundary. It does not authorize or prepare another release.

## Release Baseline

| Field | Accepted value | Observed value |
| --- | --- | --- |
| Release | `v1.0.1` final | `v1.0.1` final |
| Tag object | `7896c767376a666ad7f7d8f294beadb473b0290c` | Exact match |
| Tag target | `02f1b0113458b988562b7e03362ec9ae716cebd0` | Exact match |
| Draft | `false` | `false` |
| Prerelease | `false` | `false` |
| Assets | 6 | 6 |
| Verification | 67 checks, 0 failures | 67 checks, 0 failures |
| QEMU | Pass, blocker none, 41 markers | Pass, blocker none, 41 markers |
| Final marker | `KOZO_RUNTIME_RETURN_OK` | Exact match |

The current published release remains `v1.0.1`. The immutable `v1.0.0` and
`v1.0.0-rc.1` tag objects and hosted release records were also read back.

## Observation Period

The review covers project-visible evidence from publication at
`2026-08-07T22:45:42Z` through `2026-08-08T15:46:29Z`.

## Sources Reviewed

| Source | Result |
| --- | --- |
| GitHub issues | All open and closed project-visible issues; count 0 |
| GitHub Actions | All runs after publication; 2 successful runs |
| Hosted release | Tag, notes, classification, asset names, sizes, and digests exact |
| Hosted artifacts | Six assets downloaded and independently validated |
| Hosted ISO | Governed QEMU smoke passed with all 41 markers |
| Tagged source | Clean shallow checkout of the immutable tag target |
| Odin behavior | Local macOS compiler and hosted Linux workflow version reviewed |
| Repository task state | Existing triage and release records reviewed |

## Source Limitations

GitHub exposes only project-visible issues and workflow data. Private reports,
external conversations, and reports made outside the repository were not
available. Windows was not exercised. Hosted logs identify the Linux Odin
version and successful helper behavior, but do not record the compiler's raw
pre-normalization filename. No broader cross-host claim is made.

## Published Artifact Integrity

| Asset | Size | SHA-256 result |
| --- | ---: | --- |
| `kozo-v1.0.1.tar.xz` | 423952 | Exact match |
| `kozo.iso` | 2904064 | Exact match |
| `kozo-kernel.elf` | 74368 | Exact match |
| `latest_verify.json` | 18061 | Exact match |
| `release_metadata.json` | 604 | Exact match |
| `SHA256SUMS` | 415 | Exact match |

`SHA256SUMS` validated every listed payload. Both JSON files parsed. The
release metadata names version `1.0.1`, commit
`02f1b0113458b988562b7e03362ec9ae716cebd0`, and the expected pre-publication
bundle state `published: false`. The GitHub release record is the later
publication authority.

The downloaded ISO reached `KOZO_RUNTIME_RETURN_OK` after all 41 ordered
markers. QEMU then returned `124` because the governed smoke command timed out
after the terminal halt loop. A timeout without the complete evidence remains
a failure.

## Hosted Actions Review

| Run | Workflow | Commit | Conclusion | Finding |
| --- | --- | --- | --- | --- |
| `31226436612` | `ci` | `71cbd8651747c9088f426de20f111e49b5d2d8ee` | Success | 67 checks, QEMU pass, 41 markers |
| `31226436655` | `lint` | `71cbd8651747c9088f426de20f111e49b5d2d8ee` | Success | Static checks passed |

No failed, cancelled, timed-out, retried, or recurring post-publication run
was present. No product, infrastructure, or environment failure required a
triage case.

## Issue Inventory

| Case | Source | Surface | Status | Impact | Severity | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| None | GitHub issues and Actions | None | No report | None | None | No case created |

No qualifying project-visible defect report was found during the observation
period. This does not assert that KOZO has no defects.

## Odin/Compiler Observations

| Environment | Odin version | Requested output | Observed output | Helper result |
| --- | --- | --- | --- | --- |
| macOS | `dev-2026-07:819fdc7a8` | suffixless path | appended `.obj` | Canonical exact path |
| Hosted Linux | `dev-2026-08-nightly:902106f` | governed build path | Not logged before normalization | Build and release validation passed |
| Windows | Not executed | Not executed | Not executed | Not executed |

The macOS observation used the clean `v1.0.1` tag. The compiler emitted a
valid x86-64 ELF object at the accepted legacy `.obj` path, and
`scripts/build_odin_object.sh` normalized it to the requested canonical path.
No new form beyond exact, `.o`, or `.obj` was observed. No new
`BUILD_TOOLING` case is required.

## Reproduction Results

Two controlled baseline reproductions were run:

1. The downloaded hosted ISO reproduced QEMU pass, blocker none, all 41
   markers, and final marker `KOZO_RUNTIME_RETURN_OK`.
2. The clean tagged source reproduced accepted `.obj` compiler output and a
   successful canonical normalization.

No claimed defect existed to reproduce. No random environment permutations or
unsupported host claims were added.

## User-Impact Classification

No case was created at `U0`, `U1`, `U2`, `U3`, or `U4`. The observation found
no user-impacting regression in project-visible evidence.

## Release-Severity Classification

No case was created at `R0`, `R1`, `R2`, `R3`, or `R4`. In particular, there
is no reproduced patch candidate and no urgent security or integrity case.

## Feature Requests

No project-visible feature request was filed during the observation period.
Absent scheduler, process, filesystem, driver, network, general-userspace, and
compatibility features remain outside the `v1.0.1` scope; they are not patch
defects.

## Infrastructure Failures

No post-publication infrastructure failure was visible in GitHub Actions. The
expected QEMU timeout followed complete runtime evidence and is not an
infrastructure failure.

## Security-Sensitive Findings

No project-visible security, privilege, artifact-integrity, verification, or
runtime-evidence defect was reproduced.

## Patch-Release Decision

No reproduced patch-level defect exists. `v1.0.2` is not authorized, no
placeholder version is created, and `release/version.txt` remains `1.0.1`.

## v1.1.0 Boundary

`v1.1.0` development has not started. Any capability work requires a separate
evidence-backed scope and explicit authorization.

## Remaining Observations

Future observation should continue to watch supported Odin environments for a
new valid object-output form, recurring workflow failures, checksum or boot
regressions, and evidence-integrity failures. Windows behavior remains
unobserved. No minimum CPU, RAM, or storage profile is inferred here.

## Commands and Evidence

The observation used read-only `git` and `gh` release, issue, and Actions
queries; `gh release download`; `shasum -a 256`; `python3 -m json.tool`; `jq`;
the governed hosted-ISO QEMU smoke command; a clean tagged clone; direct Odin
object compilation; the accepted object helper; Python discovery; Bash syntax;
ShellCheck; Odin check; Cargo check; cargo-deny; cargo-audit; governed
verification; task JSON validation; and Git diff checks.

Temporary release and tagged-source observations were kept under ignored
project artifact directories. No raw report dump is committed.

## Conclusion

`v1.0.1` post-release observation is complete locally. Reproduced defects: 0.
New patch authorization: none. `v1.0.2` is not authorized. `v1.1.0` is not
started. The current release remains immutable `v1.0.1`, subject to the hosted
acceptance gate for this documentation and proof update.
