# KOZO v1.1.0 Release Evidence

Status: Final feature release prepared; publication pending

This record separates pre-publication qualification from the later immutable
GitHub release record. Hosted publication fields are completed only after the
accepted target is tagged and every downloaded asset is independently
verified.

## Release Identity

| Field | Value |
| --- | --- |
| Version | `1.1.0` |
| Display version | `v1.1.0` |
| Tag | `v1.1.0` |
| Release title | `KOZO v1.1.0` |
| Draft | `false` |
| Prerelease | `false` |

## Version Decision

`v1.0.2` is skipped. The Fixed User Execution Context and Bounded Repeated
User Session add runtime capability, so the next release is the feature
version `v1.1.0`, not a patch-only correction.

## Release Boundary

The release includes the accepted v1.0.1 behavior, Phase 0 cross-host
portability contract, Fixed User Execution Context, and exactly two bounded
user sessions. It adds no third session, process model, scheduler, allocator,
new public ABI, filesystem, driver, networking, or compatibility claim.

## Commit

The exact accepted commit and annotated tag object are recorded after the
versioned source and governed proof pass fresh hosted CI, lint, and portability
gates. Established release policy tags the hosted-accepted generated-proof
commit that follows the release-preparation commit.

## Qualification Requirements

```text
Python tests: 1,284 or greater
Odin: pass
Cargo: pass
cargo-deny: pass
cargo-audit: pass
verification: 67 checks, 0 failures
QEMU: pass
blocker: none
sessions: 2
total int 0x81 transitions: 4
markers: 52
final marker: KOZO_RUNTIME_RETURN_OK
```

Pre-version local qualification passed 337 focused tests, 1,284 full Python
tests, Odin, Rust, cargo-deny, cargo-audit, and governed verification. QEMU
reported pass with blocker none, two completed sessions, 52 ordered marker
occurrences, and `KOZO_RUNTIME_RETURN_OK` final.

## Host Contract

| Host | Build | Runtime | Evidence |
| --- | --- | --- | --- |
| Ubuntu 24.04 | Required | Required | `VALIDATED_RUNTIME` after hosted pass |
| Windows 2025 / Git Bash | Required | `NOT_EXECUTED` | `VALIDATED_BUILD` after hosted pass |
| macOS 15 | Required | `NOT_EXECUTED` | `VALIDATED_BUILD` after hosted pass |

The aggregate cross-host gate must prove identical governed release inputs.
No Windows or macOS runtime claim is made.

## Artifact List

The approved asset set is:

* `kozo-v1.1.0.tar.xz`
* `kozo.iso`
* `kozo-kernel.elf`
* `latest_verify.json`
* `release_metadata.json`
* `SHA256SUMS`

Sizes and SHA-256 values are recorded after the exact hosted-approved bundle
and independent GitHub downloads are inspected.

## Tag And Publication Rules

The tag must be annotated, unsigned, and target the exact hosted-approved
release commit. The GitHub release must be final, non-draft, and
non-prerelease. It must use `docs/releases/v1.1.0.md` and upload exactly the
six approved assets without replacement or `--clobber`.

The downloaded `SHA256SUMS` must validate the downloaded bytes. The downloaded
ISO must independently reproduce 52 ordered markers ending at
`KOZO_RUNTIME_RETURN_OK`.

## Prior Release Immutability

The existing `v1.0.1`, `v1.0.0`, and `v1.0.0-rc.1` tags, notes,
classifications, and six-asset sets remain immutable.

## Publication Status

Current published release: `v1.0.1`.

`v1.1.0` is authorized and prepared for exact-commit hosted qualification.
Publication remains pending. Generated release metadata retains
`published: false` because it records the accepted pre-publication bundle; the
later GitHub release record documents publication without rewriting the
bundle.
